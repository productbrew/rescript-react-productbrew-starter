# React with Tailwind, TypeScript, ReScript and Jest setup

## Getting started

To use this starter simply run:

```sh
npx create-react-app my-app --template https://github.com/productbrew/rescript-react-productbrew-starter
```
